name = memory-tracker
version = 2.0.0
description = Track multiple memory address types (door, health, etc.) per game with configurable monitoring
author = BizHawk Shuffler Team
bizhawk_version = >=2.8.0
setting.command_type.type = dropdown
setting.command_type.options = swap,swap_me
setting.enabled_types.type = multiselect
setting.enabled_types.options = door,health

